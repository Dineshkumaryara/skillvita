export interface CourseContent {
  courseTitle: string;
  // course: string;
  learnmoreCourse: string;
  courseDesc: string;
  keywords: string[];
  sellingPrice: number;
  costPrice: number;
  image: string;
  period: string;
  mode: string;
  // Add other properties as needed
}